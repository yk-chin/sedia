export function DegradedBanner() {
  return (
    <div className="rounded-md border border-amber-200 bg-amber-50 px-3 py-2 text-xs text-amber-800">
      正在使用离线示例数据（AI 服务暂不可用），核心计算逻辑不受影响。
    </div>
  );
}
